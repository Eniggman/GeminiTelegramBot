# 📁 Структура Проекта

> **Быстрый справочник** по архитектуре Google Gemini Telegram Bot 2.0

---

## 🗂 Файловая Структура

```
bot/
├── googlebot.py       # 🎯 Основной код бота (~3483 строки)
├── README.md          # 📖 Документация для пользователей
├── PROJECT_STRUCTURE.md # 👈 Этот файл
├── requirements.txt   # 📦 Зависимости Python (на сервере)
├── .env               # 🔐 Токены (BOT_TOKEN, GEMINI_API_KEY)
├── .gitignore         # 🚫 Игнорируемые файлы Git
├── bot.log            # 📋 Логи работы бота
├── activity_log.json  # 📊 Логи активности пользователей
├── allowed_users.json # 👥 Белый список пользователей
├── deploy.ps1         # 🚀 Скрипт деплоя на сервер
├── commit.ps1         # 📤 Скрипт коммита в Git
```

---

## 🧩 Архитектура `googlebot.py`

### 📍 Разделы файла (по строкам)

| `1-140` | **Импорты и константы** | Библиотеки, тайм-ауты, лимиты альбомов, **Системные промты (68-80)** |
| `141-221` | **Конфигурация AI** | Очистка логов, модели Gemini, инициализация |
| `222-294` | **Память и Ошибки** | Память бота, `delete_safe()`, обработка исключений |
| `295-363` | **Логирование активности** | Логи активности (Киевское время) |
| `364-448` | **Утилиты пользователей** | Доступ, аватарка, сессии Gemini |
| `449-846` | **Обработка текста** | Очистка HTML/LaTeX, форматирование, `send_safe_message` |
| `847-1177` | **Медиа-сервисы** | `generate_image()`, `edit_image()`, YouTube Саммари |
| `1178-1467` | **Команды бота** | `/start`, `/help`, `/status`, `/models`, управление доступом |
| `1468-1547` | **Обработчик голоса** | Голосовые сообщения в текст |
| `1548-2060` | **Фото и Альбомы** | `handle_photo()`, `process_album_delayed()`, `_process_photo_edit_prompt` |
| `2061-2150` | **Обработчик документов** | Анализ файлов (PDF, TXT и др.) |
| `2151-2850` | **Обработчик сообщений** | Диспетчер `handle_message()` и хелперы (Р, Пр, Ю, К) |
| `2851-3000` | **Инлайн-режим** | `handle_inline_query()`, `handle_chosen_inline_result()` |
| `3001-3483` | **Callback-и и Запуск** | `button_callback()`, `main()`, `post_init()` |

---

## 🔧 Ключевые Компоненты

### 🧠 AI Модели

```python
# Строки 94-97: Модели генерации изображений
IMAGE_MODELS = {
    'pro': 'gemini-3-pro-image-preview',
    'flash': 'gemini-2.5-flash-image'
}

# Строки 181-198: Модели чата (проверяются при старте через API)
MODELS = {
    'pro': 'gemini-3.1-pro-preview',
    'flash': 'gemini-3-flash-preview'
}

# Строки 68-80: Системные инструкции (System Prompts)
# SYSTEM_INSTRUCTION_FLASH = ...
# SYSTEM_INSTRUCTION_PRO = ...
```

### 📦 Основные Функции

| Функция                   | Строка | Назначение                   |
|:--------------------------|:------:|:-----------------------------|
| `delete_safe()`          |  260   | Безопасное удаление сообщений |
| `edit_image()`           |  877   | Редактирование фото/альбомов |
| `handle_photo()`         |  1548  | Обработка фото и альбомов    |
| `process_album_delayed()`|  1814  | Сборка и обработка альбомов  |
| `button_callback()`      |  2898  | Обработка кнопок под фото    |
| `extract_video_id()`     |  964   | Парсинг ID из YouTube ссылок |

### 🌐 Инструменты AI (Grounding)

```python
# Строки 67-69
SEARCH_TOOLS = [
    {"google_search": {}},  # Поиск в интернете
    {"url_context": {}}     # Анализ URL
]
```

### ⏱ Таймауты

| Константа               | Значение | Назначение                           |
|:------------------------|:--------:|:-------------------------------------|
| `MEMORY_TIMEOUT`        |  5 мин   | Сброс контекста чата                 |
| `TIMEOUT_SHORT`         |  60 сек  | Перевод, YouTube саммари             |
| `TIMEOUT_MEDIUM`        |  5 мин   | Gemini чат с поиском                 |
| `TIMEOUT_LONG`          | 180 сек  | Генерация/редактирование изображений |
| `PHOTO_BUTTON_TIMEOUT`  |  3 мин   | Жизнь кнопок под фото                |
| `IMAGE_CONTEXT_TIMEOUT` |  5 мин   | Жизнь изображения в контексте        |
| `ALBUM_WAIT_TIME`       | 2.5 сек  | Ожидание остальных фото альбома      |

---

## 📊 Хранение Данных

| Переменная          | Тип    | Описание                           |
|:--------------------|:-------|:-----------------------------------|
| `chat_sessions`     | `dict` | Активные сессии чата               |
| `last_activity`     | `dict` | Время последней активности         |
| `user_models`       | `dict` | Модель чата для каждого юзера      |
| `user_image_models` | `dict` | Модель генерации для каждого юзера |
| `allowed_users`     | `set`  | Белый список пользователей         |
| `bot_stats`         | `dict` | Статистика (сообщения, ошибки)     |
| `user_activity`     | `list` | Лог активности                     |

---

## 🎛 Используемые Модели (Быстрая Справка)

| Функция               | Модель          |       Переключается?        |
|:----------------------|:----------------|:---------------------------:|
| 💬 Текстовый чат      | `pro` / `flash` |    ✅ `/1model` `/2model`    |
| 🎤 Голос              | `flash`         |              ❌              |
| 🎨 Генерация картинок | `pro` / `flash` | ✅ `/imagepro` `/imageflash` |
| ✏️ Редактирование     | `pro` / `flash` |              ✅              |
| 🌐 Перевод            | `flash`         |              ❌              |
| 📺 YouTube            | `flash`         |              ❌              |
| 🔍 Инлайн             | `flash` + поиск |              ❌              |

---

## ⚡ Быстрые команды (Префиксы)

> Команды с пометкой 📝 поддерживают **два режима**:
> - Без аргумента → включает **режим ожидания** (бот ждёт следующее сообщение)
> - С текстом через пробел → **мгновенное выполнение** в одном сообщении

| Префикс               | Действие                      | Где в коде (строки) | Пример                         |
|:----------------------|:------------------------------|:----------------:|:-------------------------------|
| `п` / `про` / `pro`   | 💎 Переключить на Gemini Pro  |   `2368`         | `п`                            |
| `ф` / `флеш` / `flash`| ⚡ Переключить на Gemini Flash |   `2375`         | `ф`                            |
| `к` / `картинка`      | 🎨 Генерация изображения      | `2394` / `2426`  | `к красивый закат`             |
| `к про`               | 💎 Модель картинок → Pro      |   `2405`         | `к про`                        |
| `к флеш`              | ⚡ Модель картинок → Flash     |   `2415`         | `к флеш`                       |
| `пр` / `перевод`      | 🌐 Перевод текста/фото        | `2228` / `2237`  | `пр hello world`               |
| `р` / `редактировать` | ✏️ Режим редактирования фото  |   `2436`         | `р`                            |
| `ю` / `ютуб` / `самари`  | 📺 YouTube саммари             | `2269` / `2279`  | `ю https://youtu.be/...`       |
| `превью` / `пре`      | 🖼️ YouTube превью             | `2322` / `2332`  | `превью https://youtu.be/...`  |
| `.`                   | 🧹 Сброс контекста            |   `2382`         | `.`                            |
| `выход` / `exit`      | ⏹️ Выход из режима            |   `2186`         | `выход`                        |

---

## 🔧 Переменные окружения (.env)

| Переменная            | Обязательная? | Описание                      |
|:----------------------|:-------------:|:------------------------------|
| `TELEGRAM_TOKEN`      |       ✅       | Токен бота от @BotFather      |
| `GEMINI_API_KEY`      |       ✅       | Ключ Gemini API               |
| `ADMIN_ID`            |       ❌       | Telegram ID администратора    |
| `ALLOWED_USERS`       |       ❌       | Белый список ID через запятую |
| `LOG_TO_FILE`         |       ❌       | `1`/`0` — запись в `bot.log`  |
| `SAVE_ACTIVITY_LOG`   |       ❌       | `1`/`0` — лог активности      |
| `LOG_RETENTION_DAYS`  |       ❌       | Хранить логи N дней           |
| `LOG_MAX_TOTAL_BYTES` |       ❌       | Лимит размера логов           |

---

## 📡 Callback кнопок

| `callback_data` | Назначение              |
|:----------------|:------------------------|
| `photo_analyze` | 🔍 Анализировать фото   |
| `photo_edit`    | ✏️ Редактировать фото   |
| `album_edit`    | ✏️ Редактировать альбом |
| `photo_add_caption` | 📝 Добавить описание  |
| `img_regen`     | 🔄 Перегенерировать картинку |
| `img_new_prompt` | ✏️ Изменить промт генерации |
| `img_edit_regen` | 🔄 Перегенерировать редактирование |
| `img_edit_new_prompt` | ✏️ Изменить промт редактирования |

---

## 🔍 Инлайн-режим

> Вызов через `@bot_name текст` в любом чате

### Поддерживаемые команды

| Команда | Действие | Пример |
|:--------|:---------|:-------|
| `пр <текст>` / `перевод` | 🌐 Перевод на русский | `@bot пр hello world` |
| `ю <ссылка>` / `ютуб` | 📺 YouTube саммари | `@bot ю https://youtu.be/...` |
| `превью <ссылка>` / `пре` | 🖼️ YouTube превью | `@bot превью https://youtu.be/...` |
| `<любой текст>` | 🔮 Спросить Gemini | `@bot что такое квантовая физика` |

### Как работает

1. Пользователь пишет `@bot_name команда/вопрос`
2. `_parse_inline_command()` определяет тип команды по префиксу
3. Для `превью` — сразу получаем ID через `extract_video_id()` и отдаём фото
4. Для остальных — бот показывает кнопку (Stub), API вызывается только при нажатии (`ChosenInlineResult`)
5. Юзер кликает → в чат идёт placeholder (`⏳`)
6. `ChosenInlineResultHandler` → выполняет команду
7. Placeholder заменяется на результат

> ℹ️ **Fallback:** Если API (oEmbed) недоступен — используется стандартное превью `maxresdefault.jpg` по ID видео.

> ⚠️ **Требуется:** `/setinlinefeedback` → 100% в `@BotFather`

> ⚠️ **Важно:** `reply_markup` обязательна! Без неё Telegram не передаёт `inline_message_id`.

### Логика обработки

| Ситуация | Реакция |
|:---------|:--------|
| Нет доступа | Кнопка «Жми на меня» → переход в бота |
| Пустой запрос | Подсказка с доступными командами |
| `пр <текст>` | 🌐 Перевести → перевод через Flash |
| `ю <ссылка>` | 📺 Саммари → `summarize_youtube()` |
| `превью <ссылка>` | 🖼️ Превью → название + ссылка |
| Свободный текст | 🔮 Gemini → ответ с поиском |
| Ответ > 500 символов | `<blockquote expandable>` |

### Функции

| Функция | Назначение |
|:--------|:-----------|
| `_parse_inline_command()` | Парсер команд по префиксу |
| `handle_inline_query()` | Показ кнопок (без API) |
| `handle_chosen_inline_result()` | Выполнение + редактирование |

### Параметры

| Параметр | Значение |
|:---------|:---------|
| Модель | `flash` (фиксированная) |
| Инструменты | `google_search` + `url_context` (только Gemini) |
| Лимит ответа | 3800 символов (system_instruction) |
| Каомодзи | `ฅ≽^◕⩊◕^≼⊃━✦゜` (только Gemini) |

---

## �🔄 Режимы (`context.user_data`)

| Ключ            | Тип     | Описание                              |
|:----------------|:--------|:--------------------------------------|
| `model`         | `str`   | Текстовая модель: `'pro'` / `'flash'` |
| `image_model`   | `str`   | Модель картинок: `'pro'` / `'flash'`  |
| `mode`          | `str`   | Активный режим (см. ниже)             |
| `chat_session`  | `Chat`  | Сессия чата Gemini                    |
| `last_activity` | `float` | Timestamp последней активности        |
| `photo_task`    | `dict`  | Данные фото для кнопок                |
| `active_image`  | `dict`  | Изображение в контексте               |
| `last_image_prompt` | `str` | Последний промт генерации (для перегенерации) |

### Возможные значения `mode`:

| Режим                  | Описание                         |
|:-----------------------|:---------------------------------|
| `translate`            | Ожидание текста/фото для перевода (фото возвращается с замененным текстом) |
| `youtube_mode`         | Ожидание ссылки YouTube          |
| `image_gen`            | Ожидание промта для генерации    |
| `awaiting_edit_prompt` | Ожидание промта редактирования   |
| `awaiting_edit_photo`  | Ожидание фото для редактирования |
| `awaiting_photo_analyze_prompt` | Ожидание описания/вопроса к фото |
| `awaiting_new_image_prompt` | Ожидание нового промта генерации |
| `awaiting_new_edit_prompt` | Ожидание нового промта редактирования |

---

## 🔗 Полезные Ссылки

- [Gemini API Docs](https://ai.google.dev/gemini-api/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Python-Telegram-Bot](https://python-telegram-bot.readthedocs.io/)

